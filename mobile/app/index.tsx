import { View } from 'react-native';

/**
 * Rota de partida. Ela não desenha nada: a splash continua na tela até o
 * porteiro decidir o destino, e o destino entra sem piscar.
 */
export default function Boot() {
  return <View />;
}
