import Link from "next/link";
import { redirect } from "next/navigation";
import { getPartnerViewer } from "@/lib/auth/audience";
import { partnerHome } from "@/lib/domain/audience";
import { AudienceShell, StatusPill } from "@/components/audience/shell";
export const metadata = { title: "Partner App", robots: { index: false, follow: false } };
export default async function PartnerHomePage() { const viewer = await getPartnerViewer(); if (!viewer) redirect("/parceiro/entrar"); const data = await partnerHome(viewer.id); if (!data) redirect("/parceiro/entrar"); const fresh = data.items.filter((item) => item.status === "encaminhado"); const active = data.items.filter((item) => ["respondeu", "contato_realizado", "orcamento"].includes(item.status)); const incomplete = !data.profile?.categories.length || !data.profile.services.length; return <AudienceShell audience="partner" title="Partner App"><p className="text-brand-ink text-xs font-bold tracking-[0.14em] uppercase">{data.partner.name}</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h2 className="text-3xl">O que precisa da sua atenção?</h2><p className="text-muted mt-2">Veja pedidos relevantes e responda quando puder atender.</p></div><StatusPill tone={data.partner.status === "ativo" ? "good" : "warn"}>{data.partner.status === "ativo" ? "Recebendo oportunidades" : "Oportunidades pausadas"}</StatusPill></div>{fresh.length ? <section className="border-accent-line bg-accent-soft mt-7 rounded-2xl border p-5"><p className="text-accent-ink text-sm font-bold">Nova oportunidade</p>{fresh.slice(0, 2).map((item) => <Link key={item.id} href={`/parceiro/oportunidades/${item.id}`} className="mt-3 block rounded-xl bg-surface/75 p-4"><p className="font-semibold">{item.description}</p><p className="text-muted mt-1 text-sm">{item.neighborhood || "Canaã dos Carajás"}{item.urgency ? ` · ${item.urgency}` : ""}</p><span className="text-brand-ink mt-3 inline-block text-sm font-semibold">Ver e responder →</span></Link>)}</section> : <section className="border-line bg-surface mt-7 rounded-2xl border p-5"><p className="font-semibold">Nenhuma oportunidade nova agora.</p><p className="text-muted mt-1 text-sm">Quando houver algo compatível com seus serviços e disponibilidade, aparecerá aqui.</p></section>}{active.length ? <section className="mt-9"><h3 className="text-xl">Em andamento</h3><div className="mt-4 space-y-3">{active.map((item) => <Link key={item.id} href={`/parceiro/oportunidades/${item.id}`} className="border-line bg-surface block rounded-2xl border p-4"><p className="font-semibold">{item.description}</p><p className="text-muted mt-1 text-sm">{item.neighborhood || "Canaã dos Carajás"} · {item.categoryName || "Serviço"}</p></Link>)}</div></section> : null}{incomplete ? <section className="border-brand-line bg-brand-soft mt-9 rounded-2xl border p-5"><p className="font-semibold">Deixe seu perfil pronto</p><p className="text-muted mt-1 text-sm">Serviços e categorias completos ajudam a operação a identificar o que faz sentido enviar para você.</p><Link href="/parceiro/perfil" className="text-brand-ink mt-3 inline-block text-sm font-semibold underline underline-offset-4">Revisar perfil</Link></section> : null}</AudienceShell>; }
